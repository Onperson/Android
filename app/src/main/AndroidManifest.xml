<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.fenjiread.learner">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activity.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.activity.FlipCradActivity"/>
        <activity android:name=".activity.activity.CollectPointActivity"/>
        <activity android:name=".activity.activity.TagCloudActivity"/>
        <activity android:name=".activity.activity.SixCreditViewActivity"/>
        <activity android:name=".activity.activity.ScrollingActivity"/>
        <activity android:name=".activity.activity.DropdownViewActivity"/>
    </application>

</manifest>